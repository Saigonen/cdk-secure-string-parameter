// ~~ Generated by projen. To modify, edit .projenrc.js and run "npx projen".
import * as path from 'path';
import * as lambda from 'aws-cdk-lib/aws-lambda';
import { Construct } from 'constructs';

/**
 * Props for SecureStringParameterHandlerFunction
 */
export interface SecureStringParameterHandlerFunctionProps extends lambda.FunctionOptions {
}

/**
 * An AWS Lambda function which executes src/lambda/secure-string-parameter-handler.
 */
export class SecureStringParameterHandlerFunction extends lambda.Function {
  constructor(scope: Construct, id: string, props?: SecureStringParameterHandlerFunctionProps) {
    super(scope, id, {
      description: 'src/lambda/secure-string-parameter-handler.lambda.ts',
      ...props,
      runtime: lambda.Runtime.NODEJS_16_X,
      handler: 'index.handler',
      code: lambda.Code.fromAsset(path.join(__dirname, '../../assets/lambda/secure-string-parameter-handler.lambda')),
    });
    this.addEnvironment('AWS_NODEJS_CONNECTION_REUSE_ENABLED', '1', { removeInEdge: true });
  }
}